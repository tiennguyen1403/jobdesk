# Ruff — minimal, stable baseline (matches ruff defaults).
# To be stricter, add "I" (import sort), "B" (bugbear), "UP" (pyupgrade) to select.
target-version = "py312"
line-length = 100

[lint]
select = ["E4", "E7", "E9", "F"]
